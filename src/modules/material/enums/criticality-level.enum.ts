export enum CriticalityLevel {
  HIGH   = 'HIGH',   // critical for plant/project operations; shortage causes shutdown
  MEDIUM = 'MEDIUM', // important but short-term workarounds exist
  LOW    = 'LOW',    // standard consumables and non-critical items
}
